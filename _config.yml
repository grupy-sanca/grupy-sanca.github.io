# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: grupy-sanca
email: contato@grupysanca.com.br
description: grupo de usuários Python de São Carlos (SP)
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://grupy-sanca.github.io" # até migrarmos o .com.br pra cá fica assim

github_repo: grupy-sanca.github.io
github_username: grupy-sanca
repository: grupy-sanca/grupy-sanca.github.io

social_link: instagram
social_username: grupysanca

# Build settings
markdown: kramdown

plugins:
  - jekyll-feed
  - jekyll-remote-theme
  - jekyll-github-metadata
  - jemoji

remote_theme: broccolini/swiss
theme_color: black


# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
   - Gemfile
   - Gemfile.lock
   - README.md
   - run-jekyll.sh
